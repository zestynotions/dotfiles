#!/bin/sh
# ==================================
# = Git repo commit + push for GIT =
# ==================================

read -ep "Type GIT Commit Message: "
/usr/bin/git commit -am "${REPLY}"
/usr/bin/git push
